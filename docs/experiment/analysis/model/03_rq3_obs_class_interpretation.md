# 03 RQ-3 — Model 2 해석 방법: 관측 위치 구간과 신호 분석

## 질문 (RQ-3)

Model 2는 한 시점마다 네 예측선 `q50 / q90 / q95 / q99`를 동시에 낸다. 이 네 선을 어떻게 읽어야 하는가? 관측 첨두가 예측 밴드(`q50`~`q99`) 어디에 드는지(관측 위치 구간, obs_class)와, 그 위치를 미리 예고하는 신호는 무엇인가?

이 해석 기법 자체가 본 연구의 독립 기여물이다. 특히 관측이 밴드 최상단 `q99`마저 넘는 경우(`above_q99`)는 **모델이 첨두를 과소추정한 사건**이며, obs_class 분류가 "모델이 언제 첨두를 놓치는가"를 조건부로 진단하는 틀을 제공한다.

해석 규칙 자체는 [`quantile_output_interpretation.md`](../../method/model/quantile_output_interpretation.md)에 있고, 이 문서는 그 규칙이 실데이터에서 타당함과, 관측 위치를 예고하는 신호의 상관관계 분석 결과를 정리한다.

## 핵심 구조 — 관측 위치 구간 (obs_class)

관측 첨두가 예측 밴드 안 어디에 드는지를 다섯 서수 구간으로 나눈다.

| obs_class | 정의 | 뜻 |
| ---: | --- | --- |
| 0 | `관측 ≤ q50` | 가운데 예측보다 낮음(과대추정 쪽) |
| 1 | `q50 < 관측 ≤ q90` | 밴드 하부 |
| 2 | `q90 < 관측 ≤ q95` | 밴드 중상부 |
| 3 | `q95 < 관측 ≤ q99` | 밴드 최상부 근접 |
| 4 | `관측 > q99` | **q99도 못 잡음 = 과소추정** |

obs_class가 클수록 모델이 첨두를 더 심하게 놓친 것이다. 이 서수 변수가 RQ-3 진단의 중심 축이며, 신호 분석의 타깃(target)이 된다.

## 데이터와 사건 범위

같은 DRBC 관측 시험 split 위에서 세 사건 범위를 만들어 비교한다. 극단 사건만 보면 범위 제한(선택 편향)으로 상관관계가 왜곡되므로, Q99·NOAA 범위 외에 전체 강우 사건을 대조 범위로 사용한다.

| 범위 | 기준 | 사건 수 |
| --- | --- | --- |
| Q99 사건 | 학습기간 관측 Q99 초과 시험기간 사건 | 926건 / 82개 유역 |
| NOAA 사건 | NWS flood-stage 확인 홍수 catalog (2014-2016 × 85 basin) | 65건 / 21개 유역 |
| 전체 강우 사건 | 2014-2016 강우 반응 전체 탐지 | 16,639건 / 84개 유역 |

공통 기준:
- 평가 유역: DRBC 관측 시험 85개
- 시험 기간: 2014-01-01 ~ 2016-12-31
- Q99 임계 산정: 2000-2010 학습기간 obs 분위
- seed: `111 / 222 / 444`
- 사건 창: 관측 첨두 기준 ±6시간

## 결과 A — 관측 위치 분포

obs_class의 유역 중앙값 분포:

| obs_class | Q99 사건 (유역 중앙값) | NOAA 홍수 (유역 중앙값) |
| --- | ---: | ---: |
| `q50_to_q90` | 0.09 | 0.0 |
| `q95_to_q99` | 0.07 | 0.0 |
| **`above_q99`** | **0.469** | **1.0** |

- Q99 초과 사건의 약 **47%에서 관측이 q99마저 초과**한다(유역 중앙값).
- NOAA/NWS catalog 홍수 범위는 유역 중앙값 기준 **100%가 `above_q99`**.

τ-진행에 따른 상대 under/over-gap (Q99 사건, 유역 중앙값):

| τ | 상대 under_gap | 상대 over_gap |
| --- | ---: | ---: |
| q50 | 0.657 | 0.0 |
| q90 | 0.376 | 0.0 |
| q95 | 0.272 | 0.0 |
| q99 | 0.018 | 0.122 |

## 결과 B — 관측 위치를 예고하는 신호

후보 신호를 세 부류로 나눈다:

| 부류 | 의미 | 쓸모 |
| --- | --- | --- |
| **C 밴드 결합** | obs_class 정의에 이미 들어가 부분 순환 | 예측 신호로 쓸 수 없음 |
| **I 독립** | 외부 정보 (유역 정적 특성, 대류 지표) | 진짜 예측 신호 |
| **L 관측 누수** | 관측값 자체 (obs_peak, 관측 상승 기울기) | 기준선 (상한) — 사용 불가 |

### 독립 신호 순위 (Spearman r vs obs_class)

| 신호 | Q99 | NOAA | 전체강우 | 해석 |
| --- | ---: | ---: | ---: | --- |
| **유역 면적 area** | **+0.50** | **+0.50** | +0.27 | 범용 최강. 큰 유역일수록 obs가 밴드 위쪽 |
| **대류성 강수비 CRainf_frac** | +0.05 | **+0.39** | +0.13 | 돌발홍수에서 강력. 대류성 폭우를 모델이 못 잡음 |
| baseflow_index | +0.20 | +0.44 | +0.20 | 유역 반응성 |
| permeability | +0.32 | +0.04 | +0.26 | 투수성 |
| **CAPE** | +0.03 | +0.22 | +0.13 | 대류 잠재력 |
| 관측 첨두 obs_peak (누수 기준선 L) | +0.46 | +0.44 | — | 사용 불가. area(+0.50)가 이에 맞먹음 |

유역 면적의 예측력(+0.50)이 관측값 누수 기준선(+0.46)에 맞먹거나 더 크다 — 외부 정보만으로 과소추정 위험을 관측값만큼 잘 예고할 수 있다.

### feature 층화 분포 분석

area 사분위(Q1=소형 ~ Q4=대형)로 유역을 나눈 뒤 각 그룹의 obs_class 분포 비교:

| area 그룹 | n_events | above_q99 비율 |
| ---: | ---: | ---: |
| Q1 (소형) | 400 | 20% |
| Q2 | 285 | 40% |
| Q3 | 148 | 58% |
| Q4 (대형) | 93 | 65% |

NOAA scope에서는 단조가 더 뚜렷하다(Q1=23% → Q4=82%). 대류성 강수비 이분에서도 고 CRainf 그룹의 above_q99 비율(68%)이 저 CRainf(24%)의 약 2.8배.

### 가짜 신호 (선택 편향 · 정의상 결합)

| 신호 | Q99 | NOAA | 전체강우 | 판정 |
| --- | ---: | ---: | ---: | --- |
| rel_width (밴드 폭) | −0.14 | −0.16 | **+0.01** | 전 범위 붕괴 → 선택 편향 |
| rain_sum (강우 총량) | −0.24 | +0.08 | **−0.03** | 전 범위 ≈0 |
| seed_spread (앙상블 분산) | −0.02 | — | −0.02 | 크기 나누면 0 |

## 결과 C — 상승 기울기 신호

| 지표 | Spearman r | 비고 |
| --- | ---: | --- |
| **관측 상승 기울기 rise_slope_m4** | **0.498** | 강한 신호 — 단 관측값 누수라 모델에 사용 불가 |
| 예측 fanning_slope | −0.182 | 예측 분위 기울기 중 최강이나 약함 |
| 예측 q99_rise_slope | −0.143 | — |

관측 상승 기울기는 위험도와 강하게 연결되지만 사용 불가. 누수 없는 예측 분위 기울기는 단독으로는 신호가 약하다.

## 결과 D — 네 선 읽기 규칙의 타당성

- **예측 폭으로 읽기**: `q99 − q50` 폭이 전체 시각에서 관측의 약 122%, 관측 첨두 시각에서 절대 폭 최대
- **q50을 가운데 예측으로 읽기**: q50 중앙 성능(NSE +0.149 / RMSE −0.273 / MAE −0.197, vs Model 1) 유지
- **예측구간·보정성으로 읽지 않기**: 포함률이 모든 τ에서 명목 미달(q99 0.787 vs 0.99)이고 한쪽 → 예측구간·보정성 해석 금지가 데이터로 필요

## 범위값 한계

obs_class 상관관계는 **test set 기술(describe) 수준**이다.

- "이 상관관계를 이용해 새 사건의 분위를 사전 선택한다"는 운영 주장은 동일 test 데이터 검증이 불가하므로 이 연구 범위 밖이다.
- obs_class는 단일값이 아닌 범위(0~4)로 제공되므로, "정확히 몇 번째 분위가 맞다"는 주장 금지.
- 극단 집합(Q99/NOAA)만 보면 범위 제한으로 상관이 왜곡된다. 신호의 견고성은 반드시 전 강우 범위(3-scope)로 확인한다.

## 종합 결론

- **과소추정의 조건부 진단**: 대류성 돌발홍수 + 큰 유역에서 q99마저 넘는 과소추정이 두드러진다.
- **진짜 예측 신호**: 유역 면적·투수성·기저유출 지표(독립)와 대류성 강수비·CAPE(독립) — 극단에서 전 범위까지 살아남는다.
- **가짜 신호**: 밴드 폭·강우 총량·모델 불확실성 — 선택 편향 또는 정의상 결합 산물.

## 산출물

```text
output/model_analysis/band_signal/
├── band_shape/tables/   location_class_{q99,noaa}_summary.csv, gap_trajectory_q99_summary.csv
├── signal_sweep/tables/ branchA_spearman.csv, branchB2_spearman.csv,
│                        rq0_area_stratified_obsclass_q99.csv, rq0_crainf_stratified_obsclass_noaa.csv
├── slope_signal/tables/ quantile_rise_slope_spearman.csv
└── signal_sweep/figures/ signal_sweep_3scope.png, rq0_stratified_obsclass.png
```

## 주의점

- obs_class는 (관측 − 예측)의 위치라, 예측에서 파생된 신호(밴드 폭·분위 수준·예측 기울기)는 구조상 음의 결합을 가진다. 이런 밴드 결합(C) 신호를 예측력으로 오해하지 않는다.
- 포함률은 한쪽 형태 `P(관측 ≤ q_τ)`로 적고, 첨두 구간은 조건부 적중률로 부른다.
